<div class="ui container">
    <%= link_to "グループ作成", groups_new_path, class: "ui button right floated" %>
    <ul>
        <% @groups.each do |group| %>
            <p><%= link_to group.title, "/groups/#{group.id}" %></p>
        <% end %>
    </ul>
</div>