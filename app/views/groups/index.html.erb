<div class="ui container">
    <div class="ui clearing segment">
        <%= link_to "グループ作成", groups_new_path, class: "ui button green right floated" %>
    </div>
    <ul>
        <% @groups.each do |group| %>
            <div class="ui middle aligned divided list">
                <div class="item">
                    <div class="right floated content">
                        <div class="ui button red">退出</div>
                    </div>
                    <div class="content"><%= link_to group.title, "/groups/#{group.id}" %></div>
                </div>
            </div>
        <% end %>
    </ul>
</div>