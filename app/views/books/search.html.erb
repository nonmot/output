<h1>Search</h1>

<%= form_with url: books_result_path, method: :get, class: "ui form" do |f| %>
    <div class="field">
        <%= f.label "キーワード" %>
        <%= f.text_field :keyword %>

        <%= f.hiden_field : %>

        <%= f.submit "検索", class: "ui button" %>
    </div>
<% end %>